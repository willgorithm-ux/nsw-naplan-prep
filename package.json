{
  "name": "nsw-naplan-prep",
  "version": "1.0.0",
  "description": "NSW Year 3 NAPLAN Preparation App - Static, Frameworkless",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/generate-icons.js && rm -rf public && mkdir public && cp -r src/* public/",
    "test": "node --test",
    "test:watch": "node --test --watch src/**/*.test.js",
    "test:e2e": "playwright test",
    "lint": "echo 'Linting would run here'"
  },
  "keywords": [
    "naplan",
    "education",
    "year-3",
    "preparation",
    "nsw"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
